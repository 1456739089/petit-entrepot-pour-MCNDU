name: Fetch Paris Traffic Data

on:
  schedule:
    - cron: "0 */4 * * *"   # 每jour运行
  workflow_dispatch:

jobs:
  fetch:
    runs-on: ubuntu-latest

    steps:
      # 1. Checkout 仓库
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          persist-credentials: false
          fetch-depth: 0

      # 2. Setup Python
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      # 3. Install dependencies  ← 修复了缩进
      - name: Install dependencies
        run: |
          pip install requests pandas pyarrow

      # 4. Run fetch script
      - name: Run fetch script
        run: python scripts/fetch_traffic.py

      # 5. Git identity
      - name: Setup Git Identity
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      # 6. Add data files
      - name: Add data files
        run: |
          git add data/*.parquet || echo "No parquet files to add"

      # 7. Commit changes
      - name: Commit changes
        run: |
          git commit -m "Update traffic data at $(date -u +'%Y-%m-%dT%H:%M:%SZ')" || echo "No changes"

      # 8. Push using PAT
      - name: Push changes
        env:
          PAT_TOKEN: ${{ secrets.PAT_TOKEN }}
        run: |
          git push https://1456739089:${PAT_TOKEN}@github.com/1456739089/petit-entrepot-pour-MCNDU.git HEAD:main
